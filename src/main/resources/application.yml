spring:
  application:
    name: annotations-and-exceptions
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/annotations_and_exceptions?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: false

    open-in-view: false
logging:
  level:
    # root: WARN
    "[com.ssilvadev.api]": DEBUG
